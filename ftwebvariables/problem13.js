let saved_username = "harshdeep";
let saved_password = "abcd";

let username = "harshdeep";
let password = "abcd";

if (saved_username === username){
  if (saved_password === password){
    console.log("You are logged in!");
  }
}
else {
  console.log("Either of username or password are incorrect!");
}